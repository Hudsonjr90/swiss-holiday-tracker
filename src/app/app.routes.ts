import { Routes } from '@angular/router';
import { CalendarBaseComponent } from './calendar-base/calendar-base.component';

export const routes: Routes = [{ path: '', component: CalendarBaseComponent }];
